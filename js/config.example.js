/**
 * Faindable – Konfiguration
 *
 * Kopiere diese Datei zu `config.js` und trage deinen API Key ein.
 * Datei: js/config.js  (wird nicht in Git versioniert)
 *
 * API Key erstellen: https://console.cloud.google.com
 *   → APIs & Services → Credentials → "+ CREATE CREDENTIALS"
 *   → "PageSpeed Insights API" muss aktiviert sein
 */
window.FAINDABLE_CONFIG = {
  PAGESPEED_API_KEY: '', // z.B. 'AIzaSy...'
};
